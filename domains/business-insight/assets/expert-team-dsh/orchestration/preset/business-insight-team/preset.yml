# dsh preset 的展示元数据。**只承载显示文本。**
# id 来自目录名（必须匹配 [a-z0-9][a-z0-9-]*），trust 来自它被发现的 root，
# 两者都不可在这里写 —— 否则本地 preset 就能给自己冠上出厂 preset 的名字。
name: 业务洞察专家团
description: 十动作洞察骨架的 12 位数字专家。Lead 推进状态机，11 位队友按动作分工。
